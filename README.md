# Flask-App

## Overview

This project is a simple Flask web API for CIS 3590 that demonstrates basic backend development and RESTful API design. It exposes two endpoints, `/hello` and `/data`, both returning JSON responses. The project is developed using Agile-style iterations and version control with Git and GitHub.
